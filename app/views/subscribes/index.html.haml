%h1= t('.title')
- @subscribes.each do |s|
  = s.email
  - if can? :destroy, Subscribe
    = link_to '', '', class: 'icon-remove'
  %hr
= will_paginate @subscribes

%button.pure-button.pure-button-success= link_to t('main.index.subscribe'), new_subscribe_path, class: 'btn btn-success btn'